<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testing.draw">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ondraw.panel.PanelActivity" />
        <activity android:name=".colormatrix.ColorMatrixApiActivity" />
        <activity android:name=".colormatrix.ColorMatrixActivity" />
        <activity android:name=".colormatrix.ImagePixelsActivity" />
        <activity android:name=".matrix.MatrixApiActivity" />
        <activity android:name=".mesh.BitmapMeshActivity" />
        <activity android:name=".porterDuffXfermode.PorterDuffXfermodeActivity_01" />
        <activity android:name=".shader.ShaderActivity"></activity>
    </application>

</manifest>